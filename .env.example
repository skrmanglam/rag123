# LLM — match provider in config/settings.yaml

# OpenRouter (default for HF Spaces / cloud demo)
OPENROUTER_API_KEY=sk-or-your-key-here

# OpenAI (if provider is "openai")
# OPENAI_API_KEY=sk-your-openai-key-here

# Ollama (if provider is "ollama") — no key; run: ollama serve